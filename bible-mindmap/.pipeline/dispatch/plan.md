# 파이프라인 작업 계획

## 배치별 현재 단계와 남은 단계

- batch 03 · task7: 제안 12/15, 대조 완료, 판정 3건·감사 4건·재감사 완료. 남은 단계는 배치 파일 생성·검증·PR이다.
- batch 04 · task8: 제안 12/15, 대조 완료, 판정 3건·감사 4건·재감사 완료. 남은 단계는 배치 파일 생성·검증·PR이다.
- batch 05 · task9: 제안 15/15, 대조 완료, 판정 3건·감사 4건·재감사 완료. 남은 단계는 배치 파일 생성·검증·PR이다.
- batch 06 · task10: 제안 15/15, 대조 완료, 판정 3건·감사 4건·재감사 완료. 남은 단계는 배치 파일 생성·검증·PR이다.
- batch 07 · task11: 제안 15/15, 대조 완료, 판정 대기. 판정 3건, 감사 4건, 재감사 1건, 배치 파일 생성·검증·PR이 남았다.

## 이번에 jobs.tsv에 넣은 일

- 추가 없음. batch 07 판정 3건은 이미 `jobs.tsv`에 등록되어 있고, 세 프롬프트도 모두 존재한다.

## 0-lead가 해야 할 일

- batch 07 판정 3건이 끝나면 규칙을 적용하고 `final.json` 생성 조건을 확인한다.
- batch 07 감사 4건과 재감사 1건을 순서대로 실행한다.
- batch 03·04·05·06의 배치 파일 생성·검증·PR을 진행한다.
- batch 07 재감사 완료 후 배치 파일 생성·검증·PR을 진행한다.

## 막힌 곳 및 이상 징후

- 감독관이 추가할 작업은 없다. 현재 디스패처 대기 작업은 3건이며 모두 기존 목록에 있다.
- `.pipeline/dispatch/dispatch.log`에서 batch 07 R · Codex GPT-5가 산출물 미생성으로 1회 재배정된 기록이 있다. 동일 산출물이 2회 이상 재배정된 흔적은 확인되지 않았다.
- `report.md`는 직전 상태와 현재 `state.mjs` 출력 사이에 단계 변화가 확인되지 않아 다시 쓰지 않았다.
