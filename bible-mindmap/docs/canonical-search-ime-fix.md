# 정경 추적 검색 IME 입력 안정화

- 검색창의 로컬 React 상태는 키 입력마다 즉시 갱신한다.
- NVIDIA 비교용 부모 상태는 입력이 700ms 멈춘 뒤에만 갱신한다.
- 한글 조합 중(`isComposing`)에는 부모 상태와 shadow 호출을 건드리지 않는다.
- `compositionend` 이후 완성된 문자열만 비교 예약에 전달한다.
- 목적은 한글 자모 조합, 커서 위치, 포커스, 입력 반응성을 보존하는 것이다.
